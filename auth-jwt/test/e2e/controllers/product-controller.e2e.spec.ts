import { context } from "../../helpers/context";

describe("ProductController - e2e test", () => {
  context("find", () => {});
  context("create", () => {});
  context("update", () => {});
  context("delete", () => {});
});
